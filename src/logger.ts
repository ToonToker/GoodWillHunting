export const logInfo = (m: string): void => console.log(`[INFO] ${m}`);
export const logWarn = (m: string): void => console.warn(`[WARN] ${m}`);
export const logError = (m: string): void => console.error(`[ERROR] ${m}`);
